<% @user ||= current_user %>
<div class="stats">
  <a href="<%= players_user_path(@user) %>">
    <strong id="followers" class="stat">
      <%= @user.players.count %>
    </strong>
    followers
  </a>
</div>