<%= form_for(current_user.relationships.build(user_id: current_user.id)) do |f| %>
    <div><%= f.hidden_field :player_id %></div>
    <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>